# FORUM — Lemmy. Phase 4 placeholder.
#
# Right now this serves a static "coming soon" page from the homepage
# container. When Lemmy lands, delete the two lines marked PLACEHOLDER and
# replace them with a normal proxy_pass — nothing else in the proxy changes.
#
#     location /forum/ {
#         set $forum_upstream http://lemmy-ui:1234;
#         proxy_pass $forum_upstream;
#         include /etc/nginx/snippets/proxy-headers.conf;
#         include /etc/nginx/snippets/websocket.conf;
#     }

location /forum {
    set $homepage_upstream http://homepage:80;           # PLACEHOLDER
    proxy_pass $homepage_upstream/forum.html;            # PLACEHOLDER
    include /etc/nginx/snippets/proxy-headers.conf;
}
