# HOMEPAGE — "/" catch-all. Static files served by the homepage container.
# Anything not claimed by a more specific location below lands here.

location / {
    set $homepage_upstream http://homepage:80;
    proxy_pass $homepage_upstream;
    include /etc/nginx/snippets/proxy-headers.conf;
}
